# Colab workflow

Use Colab as a thin runner, not as the project home.

Suggested notebook steps:
1. Clone your GitHub repo.
2. Install dependencies.
3. Run `python -m src.train --config configs/baseline.yaml`.
4. Save `runs/` outputs and metrics.
5. Push results or upload artifacts.
